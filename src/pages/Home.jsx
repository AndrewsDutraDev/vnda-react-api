import { Form } from '../components/form';

function Home() {
	// const token = useContext(MyContext);
	return (
		<>
			<div className="container">
				<Form/>
			</div>
		</>
	)
}

export default Home
